import React from 'react';
import { Text, View } from 'react-native';

export default function LoginScreen() {
  return <View style={{padding:16}}><Text>Login / Sign up screen (MVP scaffold).</Text></View>;
}
